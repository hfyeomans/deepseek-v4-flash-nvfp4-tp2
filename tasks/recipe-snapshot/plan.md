# Clean recipe snapshot checks

The source history is archived privately before replacing recipe refs.
Keep serving code, clients, patches and measured acceleration records unchanged.
Remove research-history administration from the recipe export, reconcile
rollback/provenance instructions and verify the existing client/profile tests.
Check links, JSON, source/result hashes and every published branch/tag before
use as a release source. Never merge old combined ancestry into this repository.
