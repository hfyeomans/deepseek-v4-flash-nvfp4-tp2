# Two-pass documentation edit

Start from release checkpoint `0b67089`. Review every authored Markdown document
in the recipe. Keep raw results, benchmark fixtures, licenses and executable
files unchanged. Preserve every table, visual, code block, command and source
link. Keep measurements and the limits of each experiment intact.

1. Humanizer: cut filler, repeated explanations and awkward technical prose.
2. Hank's voice, technical mode: add direct phrasing, contractions and the
   learning motivation already established in this project. Don't invent
   personal experience, tests or stronger conclusions.
3. Verify protected content and links, compare both passes, then commit the
   finished edit for review. Keep the repository private.

Retain a separate checkpoint after each pass. Shorten prose where it helps;
don't force a reduction target onto necessary instructions or evidence.
